\title{#{F}-algebra (algebra for an endofunctor)}
\taxon{definition}
\author{dannypsnl}
\author{fizzyelt}


\p{在一個適當的範疇 #{C} 中，我們可以把簽名寫成 #{F(-)} 這個函子投射出的 #{F(m) \to m} 是 #{F}-algebra，可以記為三元組：}

##{(F,x,\alpha)}

\p{其中 #{\alpha : Fx \to x} 是 #{C} 中的一個態射。}

\p{functor 與 natural transformation 構成 functor category}
