\title{#{F}-algebra (algebra for an endofunctor)}
\taxon{definition}
\author{dannypsnl}
\author{fizzyelt}


\p{在一個適當的範疇 #{C} 中，可以描述一個 #{F(-)} 符號，以及一個自函子 #{F:C \to C}，而 #{F}-algebra 為一個三元組：}

##{(F,x,\alpha)}

\p{其中 #{\alpha : Fx \to x} 是 #{C} 中的一個態射。}

\p{根據 #{F}-algebra 的定義，我們想知道固定 #{F} 形式的 #{F}-algebra 是否形成一個範疇，答案是肯定的。}

